import { MOVIES } from '../data.js';import { addPts, save, state } from '../app.js';
export function renderCulture(){setTimeout(bindCulture);return `<!-- PAGE CULTURE --><div class="card"><h2>Culture</h2><input class="input" id="search" placeholder="Rechercher"><div class="wrap" id="cats">${['Toutes',...new Set(MOVIES.map(m=>m.cat))].map(c=>`<button class="chip" data-cat="${c}">${c}</button>`).join('')}</div></div><div id="movieList"></div>`}
function bindCulture(){let cat='Toutes';function draw(){let q=document.getElementById('search').value.toLowerCase();let arr=MOVIES.filter(m=>(cat==='Toutes'||m.cat===cat)&&(m.title.toLowerCase().includes(q)||m.sub.toLowerCase().includes(q)));document.getElementById('movieList').innerHTML=arr.map(m=>`<div class="card"><div class="row"><b>${m.title}</b><span class="pill">${m.status}</span></div><div class="wrap"><span class="pill">${m.cat}</span><span class="pill">${m.sub}</span></div><button class="chip" data-watch="${m.title}">Vu + points</button><button class="chip" data-fav="${m.title}">Favori</button></div>`).join('')}document.getElementById('search').oninput=draw;document.querySelectorAll('[data-cat]').forEach(b=>b.onclick=()=>{cat=b.dataset.cat;document.querySelectorAll('[data-cat]').forEach(x=>x.classList.remove('sel'));b.classList.add('sel');draw()});document.getElementById('movieList').onclick=e=>{if(e.target.dataset.watch){if(!state.watched.includes(e.target.dataset.watch))state.watched.push(e.target.dataset.watch);addPts(50);save();alert('Film validé')}if(e.target.dataset.fav){if(!state.favs.includes(e.target.dataset.fav))state.favs.push(e.target.dataset.fav);save();alert('Ajouté aux favoris')}};draw()}