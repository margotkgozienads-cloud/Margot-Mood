import { addPts, save, pct, level, nextLevel, statCard, go, state } from '../app.js';
export function renderHome(s){setTimeout(bindHome);const today=s.todos.filter(t=>t.date==='today');return `<!-- PAGE ACCUEIL --><div class="card"><p class="muted">Bonjour Margot</p><div class="row"><div><div class="big">Niveau ${level()}</div><span class="pill">${s.points} points · prochain palier ${nextLevel()}</span></div><button class="btn" id="surprise">Surprise culture</button></div><div class="progress" style="margin-top:12px"><div class="bar" style="width:${pct(s.points,nextLevel())}%"></div></div></div><h2 class="sectionTitle">Inspirations</h2><div class="grid grid2"><button class="card soft" id="anti">🔒 Anti-scroll</button><button class="card soft" id="moodGo">🧠 Mood</button><button class="card soft" id="softTask">🌿 Tâche douce</button><button class="card soft" id="cultureGo">🎬 Surprise culture</button></div><h2 class="sectionTitle">Récapitulatif complet</h2><div class="grid grid2">${statCard('Points',s.points)}${statCard('Sport',s.sport.week+'/4 semaine')}${statCard('Skincare',(s.daily.skincare?1:0)+'/7')}${statCard('Pas',s.steps+'/10k')}${statCard('To-do',today.filter(t=>!t.done).length+' aujourd’hui')}${statCard('Stretching',s.sport.stretch+'/7')}${statCard('Calories',s.calories)}${statCard('Protéines',s.proteins+'g')}</div><h2 class="sectionTitle">Quotidien</h2><div class="card"><label class="item"><span>Skincare</span><input type="checkbox" data-daily="skincare" ${s.daily.skincare?'checked':''}></label><label class="item"><span>Stretching</span><input type="checkbox" data-daily="stretching" ${s.daily.stretching?'checked':''}></label><label class="item"><span>2L d’eau</span><input type="checkbox" data-daily="water" ${s.daily.water?'checked':''}></label><div class="row"><b>Pas</b><span>${s.steps} / 10000</span></div><div class="progress"><div class="bar" style="width:${pct(s.steps,10000)}%"></div></div><input class="input" id="steps" type="number" value="${s.steps}"></div><h2 class="sectionTitle">Agenda aujourd’hui et demain</h2>${['today','tomorrow'].map(d=>`<div class="card"><b>${d==='today'?'Aujourd’hui':'Demain'}</b>${s.agenda.filter(a=>a.day===d).map(a=>`<p>${a.time} · ${a.title}</p>`).join('')||'<p class="muted">Rien de prévu</p>'}</div>`).join('')}`}
function bindHome(){document.getElementById('moodGo').onclick=()=>go('mood');document.getElementById('cultureGo').onclick=()=>go('culture');document.getElementById('surprise').onclick=()=>go('culture');document.getElementById('anti').onclick=()=>document.body.classList.toggle('locked');document.getElementById('softTask').onclick=()=>alert('Tâche douce : ranger un seul petit coin pendant 7 minutes.');document.querySelectorAll('[data-daily]').forEach(c=>c.onchange=e=>{state.daily[e.target.dataset.daily]=e.target.checked;if(e.target.checked)addPts(25);save();go('home')});document.getElementById('steps').onchange=e=>{state.steps=+e.target.value||0;save();go('home')}}