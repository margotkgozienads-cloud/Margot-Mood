import { IDEAS } from '../data.js';import { addPts } from '../app.js';
export function renderMood(){setTimeout(bindMood);return `<!-- PAGE MOOD --><div class="card"><h2>Comment je me sens ?</h2><div class="wrap" id="feel">${['fatiguée','stressée','motivée','seule','créative','triste'].map(x=>`<button class="chip">${x}</button>`).join('')}</div><h2>De quoi j’ai envie ?</h2><div class="wrap" id="want">${['réconfort','calme','bouger','lien','inspiration','douceur'].map(x=>`<button class="chip">${x}</button>`).join('')}</div><button class="btn" id="idea">Me donner une seule idée</button></div><div class="card soft" id="result"><b>Ton idée apparaîtra ici.</b></div>`}
function bindMood(){let f='',w='';document.querySelectorAll('#feel .chip').forEach(b=>b.onclick=()=>{f=b.textContent;document.querySelectorAll('#feel .chip').forEach(x=>x.classList.remove('sel'));b.classList.add('sel')});document.querySelectorAll('#want .chip').forEach(b=>b.onclick=()=>{w=b.textContent;document.querySelectorAll('#want .chip').forEach(x=>x.classList.remove('sel'));b.classList.add('sel')});document.getElementById('idea').onclick=()=>{let m=IDEAS.find(i=>i[0]==f||i[1]==w)||IDEAS[Math.floor(Math.random()*IDEAS.length)];document.getElementById('result').innerHTML=`<b>${m[2]}</b><p class="muted">Si ça ne te parle pas, reclique.</p>`;addPts(15)}}