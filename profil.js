import { save } from '../app.js';
export function renderProfil(root,state){root.innerHTML=`<section class="grid grid2"><div class="card"><b>Niveau</b><div class="big">${state.level}</div></div><div class="card"><b>Points</b><div class="big">${state.points}</div></div><div class="card"><b>Culture</b><div>${state.points>=0?'Films suivis via points':''}</div></div><div class="card"><b>Sport</b><div>${state.sport.month} séances mois · ${state.sport.kmMonth} km</div></div></section><h2 class="sectionTitle">Récompenses achetables</h2><section class="list">${state.rewards.map((r,i)=>`<div class="item row reward"><span>${r.name}<br><small>${r.cost} points</small></span><button class="chip" data-buy="${i}">${r.bought?'Acheté':'Acheter'}</button></div>`).join('')}</section><h2 class="sectionTitle">Sauvegarde</h2><section class="card grid"><button class="btn secondary" id="export">Exporter JSON</button><textarea class="input textarea" id="backup" placeholder="Colle une sauvegarde ici pour restaurer"></textarea><button class="btn" id="import">Restaurer</button><button class="chip danger" id="reset">Réinitialiser</button></section>`;root.querySelectorAll('[data-buy]').forEach(b=>b.onclick=()=>{const r=state.rewards[b.dataset.buy];if(!r.bought&&state.points>=r.cost){state.points-=r.cost;r.bought=true;save(state);renderProfil(root,state)}else alert('Pas assez de points ou déjà acheté.')});root.querySelector('#export').onclick=()=>{root.querySelector('#backup').value=JSON.stringify(state,null,2)};root.querySelector('#import').onclick=()=>{try{localStorage.setItem('margotMoodV8',root.querySelector('#backup').value);location.reload()}catch(e){alert('Sauvegarde invalide')}};root.querySelector('#reset').onclick=()=>{localStorage.removeItem('margotMoodV8');location.reload()}}
