import { DEFAULT_STATE, LEVELS } from './data.js';
import { renderHome } from '../pages/home.js';import { renderMood } from '../pages/mood.js';import { renderHobbies } from '../pages/hobbies.js';import { renderSport } from '../pages/sport.js';import { renderNutrition } from '../pages/nutrition.js';import { renderTodo } from '../pages/todo.js';import { renderCulture } from '../pages/culture.js';import { renderListPage } from '../pages/simpleList.js';import { renderProfil } from '../pages/profil.js';
const KEY='margotMoodV8PagesReprise';
export const root=document.getElementById('root');export const title=document.getElementById('pageTitle');
export const PAGES={home:{label:'Accueil',icon:'🏠',render:renderHome},mood:{label:'Mood',icon:'🧠',render:renderMood},hobbies:{label:'Hobbies',icon:'✨',render:renderHobbies},sport:{label:'Sport',icon:'🏃',render:renderSport},nutrition:{label:'Nutrition',icon:'🥗',render:renderNutrition},todo:{label:'To-do',icon:'✅',render:renderTodo},culture:{label:'Culture',icon:'🎬',render:renderCulture},courses:{label:'Courses',icon:'🛒',render:(s)=>renderListPage(s,'courses','Courses','Nouvel article')},appart:{label:'Appart',icon:'🏡',render:(s)=>renderListPage(s,'appart','Appart','Nouvelle tâche appart')},envies:{label:'Envies',icon:'💛',render:(s)=>renderListPage(s,'envies','Envies','Nouvelle envie')},profil:{label:'Profil',icon:'👤',render:renderProfil}};
export function load(){return JSON.parse(localStorage.getItem(KEY)||'null')||structuredClone(DEFAULT_STATE)}
export let state=load();export function save(){localStorage.setItem(KEY,JSON.stringify(state))}export function setState(s){state=s;save()}export function addPts(n){state.points+=n;save()}export function level(){let l=1;LEVELS.forEach((v,i)=>{if(state.points>=v)l=i+1});return l}export function nextLevel(){return LEVELS[level()]||LEVELS[LEVELS.length-1]}export function pct(v,m){return Math.min(100,Math.round(v/m*100))}export function statCard(t,v){return `<div class="card stat"><b>${t}</b><div>${v}</div></div>`}export function go(p){state=load();document.querySelectorAll('#nav button').forEach(b=>b.classList.toggle('active',b.dataset.p===p));title.textContent=PAGES[p].label;root.innerHTML=PAGES[p].render(state);if(PAGES[p].bind)PAGES[p].bind();}
const nav=document.getElementById('nav');nav.innerHTML=Object.entries(PAGES).map(([k,p])=>`<button data-p="${k}">${p.icon}<span>${p.label}</span></button>`).join('');nav.querySelectorAll('button').forEach(b=>b.onclick=()=>go(b.dataset.p));document.getElementById('lockBtn').onclick=()=>document.body.classList.toggle('locked');go('home');
