import { save, addPoints } from '../app.js';
export function renderHobbies(root,state){const keys=Object.keys(state.hobbies);root.innerHTML=`<section class="card"><h2>Hobbies</h2><p class="muted">Version points : chaque action validée ajoute 20 points.</p><div class="list">${keys.map(k=>`<div class="item row"><span>${k}</span><span><b>${state.hobbies[k]}</b> fois <button class="chip" data-hobby="${k}">+1</button></span></div>`).join('')}</div></section>`;root.querySelectorAll('[data-hobby]').forEach(b=>b.onclick=()=>{state.hobbies[b.dataset.hobby]++;addPoints(state,20);save(state);renderHobbies(root,state)})}
